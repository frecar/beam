# Beam Remote Desktop Configuration

[server]
bind = "0.0.0.0"
port = 8444
# web_root = "/usr/share/beam/web/dist"  # Default: "web/dist" (relative to CWD)
# tls_cert = "/etc/beam/cert.pem"    # Auto-generated if absent
# tls_key = "/etc/beam/key.pem"      # Auto-generated if absent

[video]
bitrate = 5000      # kbps (initial target)
min_bitrate = 500   # kbps (adaptive lower bound)
max_bitrate = 20000 # kbps (adaptive upper bound)
framerate = 120
# encoder = "nvh264enc"  # Auto-detected if absent: nvh264enc > vah264enc > x264enc

[audio]
enabled = true
bitrate = 128       # kbps (Opus)

[session]
default_width = 1920
default_height = 1080
display_start = 10
max_sessions = 8
